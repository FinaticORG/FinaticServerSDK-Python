# coding: utf-8

"""
    Finatic FastAPI Backend

    FinaticAPI REST API

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class BrokerPermissions(BaseModel):
    """
    Broker permissions model.  This model defines the permission levels for accessing broker connection data and performing trading operations. Used in BrokerConnectionRequest and BrokerConnectionUpdateRequest to specify what actions a company account can perform with a broker connection.  Permissions are stored in the CompanyAccess table and control access at the company level, allowing fine-grained control over which companies can read data versus execute trades.  Attributes ---------- read : bool     Whether the company has permission to read broker data including positions,     orders, account information, and market data. Default is True. When False,     the company cannot access any broker data through this connection. write : bool     Whether the company has permission to place trades, modify orders, and     perform other write operations. Default is False. When True, the company     can execute trading operations through this broker connection.  Notes ----- 1. **Permission Combinations**:    - read=True, write=False: Read-only access (view positions, orders, accounts)    - read=True, write=True: Full access (read data and execute trades)    - read=False, write=False: No access (connection effectively disabled for company)    - read=False, write=True: Invalid combination (write requires read access)  2. **Default Behavior**: By default, connections grant read-only access. Write    permissions must be explicitly enabled.  3. **Company-Level Control**: Permissions are scoped to individual company    accounts. The same broker connection can have different permissions for    different companies.  4. **Security**: Write permissions enable trading operations. Use with caution    and ensure proper authorization checks are in place.  Examples -------- >>> # Read-only permissions (default) >>> read_only = BrokerPermissions(read=True, write=False) >>> # Full trading access >>> full_access = BrokerPermissions(read=True, write=True) >>> # Explicit read-only (same as default) >>> explicit_read_only = BrokerPermissions() >>> # Use in connection request >>> request = BrokerConnectionRequest( ...     broker_id=\"tradestation\", ...     credentials={\"api_key\": \"xxx\"}, ...     permissions=BrokerPermissions(read=True, write=True), ... )  See Also -------- BrokerConnectionRequest : Request model that includes permissions BrokerConnectionUpdateRequest : Update request model that can modify permissions finaticapi.core.services.broker_service.BrokerService : Service methods that enforce permissions
    """ # noqa: E501
    read: Optional[StrictBool] = Field(default=True, description="Access to read data (positions, orders, accounts)")
    write: Optional[StrictBool] = Field(default=False, description="Access to place trades")
    __properties: ClassVar[List[str]] = ["read", "write"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of BrokerPermissions from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of BrokerPermissions from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "read": obj.get("read") if obj.get("read") is not None else True,
            "write": obj.get("write") if obj.get("write") is not None else False
        })
        return _obj
