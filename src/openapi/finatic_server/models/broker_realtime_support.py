# coding: utf-8

"""
    Finatic FastAPI Backend

    FinaticAPI REST API

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class BrokerRealtimeSupport(BaseModel):
    """
    Realtime support for one broker resource.
    """ # noqa: E501
    id: Optional[StrictStr] = Field(default=None, alias="_id")
    caveats: Optional[List[StrictStr]] = None
    fallback_mode: Optional[StrictStr] = 'polling'
    idempotent_status_events: Optional[StrictBool] = True
    mode: Optional[StrictStr] = 'polling'
    resource: StrictStr
    __properties: ClassVar[List[str]] = ["_id", "caveats", "fallback_mode", "idempotent_status_events", "mode", "resource"]

    @field_validator('fallback_mode')
    def fallback_mode_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['polling', 'none']):
            raise ValueError("must be one of enum values ('polling', 'none')")
        return value

    @field_validator('mode')
    def mode_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['stream', 'webhook', 'polling', 'unavailable']):
            raise ValueError("must be one of enum values ('stream', 'webhook', 'polling', 'unavailable')")
        return value

    @field_validator('resource')
    def resource_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['orders', 'positions', 'balances', 'transactions', 'accounts']):
            raise ValueError("must be one of enum values ('orders', 'positions', 'balances', 'transactions', 'accounts')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of BrokerRealtimeSupport from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of BrokerRealtimeSupport from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "_id": obj.get("_id"),
            "caveats": obj.get("caveats"),
            "fallback_mode": obj.get("fallback_mode") if obj.get("fallback_mode") is not None else 'polling',
            "idempotent_status_events": obj.get("idempotent_status_events") if obj.get("idempotent_status_events") is not None else True,
            "mode": obj.get("mode") if obj.get("mode") is not None else 'polling',
            "resource": obj.get("resource")
        })
        return _obj
