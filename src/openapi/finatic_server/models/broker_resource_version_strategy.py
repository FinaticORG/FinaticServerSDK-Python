# coding: utf-8

"""
    Finatic FastAPI Backend

    FinaticAPI REST API

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class BrokerResourceVersionStrategy(BaseModel):
    """
    Public resource version metadata used to reject stale updates.
    """ # noqa: E501
    id: Optional[StrictStr] = Field(default=None, alias="_id")
    exposes_public_version: Optional[StrictBool] = True
    metadata_fields: Optional[List[StrictStr]] = None
    resource: StrictStr
    stale_update_policy: Optional[StrictStr] = 'reject'
    version_source: Optional[StrictStr] = 'finatic_timestamp'
    __properties: ClassVar[List[str]] = ["_id", "exposes_public_version", "metadata_fields", "resource", "stale_update_policy", "version_source"]

    @field_validator('resource')
    def resource_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['orders', 'positions', 'balances', 'transactions', 'accounts']):
            raise ValueError("must be one of enum values ('orders', 'positions', 'balances', 'transactions', 'accounts')")
        return value

    @field_validator('stale_update_policy')
    def stale_update_policy_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['reject', 'latest_wins', 'append']):
            raise ValueError("must be one of enum values ('reject', 'latest_wins', 'append')")
        return value

    @field_validator('version_source')
    def version_source_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['broker_sequence', 'broker_updated_at', 'finatic_timestamp']):
            raise ValueError("must be one of enum values ('broker_sequence', 'broker_updated_at', 'finatic_timestamp')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of BrokerResourceVersionStrategy from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of BrokerResourceVersionStrategy from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "_id": obj.get("_id"),
            "exposes_public_version": obj.get("exposes_public_version") if obj.get("exposes_public_version") is not None else True,
            "metadata_fields": obj.get("metadata_fields"),
            "resource": obj.get("resource"),
            "stale_update_policy": obj.get("stale_update_policy") if obj.get("stale_update_policy") is not None else 'reject',
            "version_source": obj.get("version_source") if obj.get("version_source") is not None else 'finatic_timestamp'
        })
        return _obj
