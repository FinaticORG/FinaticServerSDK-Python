# coding: utf-8

"""
    Finatic FastAPI Backend

    FinaticAPI REST API

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self

class FDXWebhookSubscriptionCreate(BaseModel):
    """
    Create a webhook subscription for customer event delivery.
    """ # noqa: E501
    id: Optional[StrictStr] = Field(default=None, alias="_id")
    description: Optional[StrictStr] = None
    environment: Optional[StrictStr] = 'live'
    event_types: Annotated[List[StrictStr], Field(min_length=1)] = Field(alias="eventTypes")
    url: Annotated[str, Field(min_length=1, strict=True, max_length=2083)]
    __properties: ClassVar[List[str]] = ["_id", "description", "environment", "eventTypes", "url"]

    @field_validator('environment')
    def environment_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['live', 'sandbox']):
            raise ValueError("must be one of enum values ('live', 'sandbox')")
        return value

    @field_validator('event_types')
    def event_types_validate_enum(cls, value):
        """Validates the enum"""
        for i in value:
            if i not in set(['account.grant.created', 'account.grant.updated', 'account.grant.revoked', 'account.sync.started', 'account.sync.succeeded', 'account.sync.failed', 'account.balance.updated', 'account.position.updated', 'account.transaction.created', 'order.created', 'order.updated', 'order.filled', 'order.cancelled', 'order.rejected', 'connection.reauth.required']):
                raise ValueError("each list item must be one of ('account.grant.created', 'account.grant.updated', 'account.grant.revoked', 'account.sync.started', 'account.sync.succeeded', 'account.sync.failed', 'account.balance.updated', 'account.position.updated', 'account.transaction.created', 'order.created', 'order.updated', 'order.filled', 'order.cancelled', 'order.rejected', 'connection.reauth.required')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of FDXWebhookSubscriptionCreate from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if description (nullable) is None
        # and model_fields_set contains the field
        if self.description is None and "description" in self.model_fields_set:
            _dict['description'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of FDXWebhookSubscriptionCreate from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "_id": obj.get("_id"),
            "description": obj.get("description"),
            "environment": obj.get("environment") if obj.get("environment") is not None else 'live',
            "eventTypes": obj.get("eventTypes"),
            "url": obj.get("url")
        })
        return _obj
